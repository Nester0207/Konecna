while(true) {
    let odpovedKlienta = prompt("Jak se jmenujete?").toLowerCase();
    if (odpovedKlienta != "martin" && odpovedKlienta != "martin"){
        alert("Zadali jste spatnou odpoved, zkuste to znova");
    }else{
        break;
    }
}